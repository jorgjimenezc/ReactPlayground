import React from 'react';
import ReactDOM from 'react-dom';
import { App } from './App';


//import {BotesPinturaApp} from './BotesPinturaApp';

//import './index.css';
//import reportWebVitals from './reportWebVitals';

ReactDOM.render(
  
    <App />,
  document.getElementById('root')
);


/**
 * TERMINOS PARA GIT en el Terminal.
 * 1. git STATUS : revisa el estado de los commits (si estan guardados o no).
 * 2. git LOG    : muestra los ultimos commits ( cambios guardados del proyecto).
 * 3. git COMMIT : guarda los cambios al reporsitorio local. 
 * 4. git Commit -m : guarda y agrega una nota de lo que guardamos
 * 5. git push   : sube o envia los commits lcoal al repositorio principal. 
 * 6. git reset  : averiguar.
 */